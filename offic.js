// Require the 'fs' module at the beginning of your file
const fs = require('fs');

// Define the fileLoader function using the 'fs' module
function fileLoader(fs) {
  // Your code here
}

// Export the function if you intend to use it in other modules
module.exports = fileLoader;
